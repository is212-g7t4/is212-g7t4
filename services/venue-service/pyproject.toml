[project]
name = "venue-service"
version = "0.1.0"
description = "Venue catalogue read service"
requires-python = ">=3.12"
dependencies = ["flask>=3.1.3", "psycopg2-binary>=2.9", "python-dotenv>=1.0.1"]

[dependency-groups]
dev = ["pytest>=9.1.1"]

[tool.pytest.ini_options]
pythonpath = ["."]
