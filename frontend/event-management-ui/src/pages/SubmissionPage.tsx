import type { EventData } from '../types'
import { Field, FormSection } from '../components/FormControls'

export function SubmissionPage({ event, updateEvent, onSubmit }: { event: EventData; updateEvent: (field: keyof EventData, value: string) => void; onSubmit: () => void }) {
  return <div className="page-stack"><section className="intro"><p className="muted">Create a mock request with the details Event services will eventually own.</p></section><form className="panel form-panel" onSubmit={(submitEvent) => { submitEvent.preventDefault(); onSubmit() }}><FormSection title="Event basics" hint="Required fields from the event request story."><Field label="Event title" value={event.title} onChange={(value) => updateEvent('title', value)} required /><Field label="Description" value={event.description} onChange={(value) => updateEvent('description', value)} textarea required /><Field label="Purpose" value={event.purpose} onChange={(value) => updateEvent('purpose', value)} textarea required /></FormSection><FormSection title="Date and capacity"><div className="field-row"><Field label="Preferred date and time" value={event.date} onChange={(value) => updateEvent('date', value)} required /><Field label="Expected attendance" value={event.attendance} onChange={(value) => updateEvent('attendance', value)} required /></div></FormSection><FormSection title="Requirements" hint="These optional requests help shape the event plan."><Field label="Venue request" value={event.venue} onChange={(value) => updateEvent('venue', value)} textarea /><Field label="Requested equipment" value={event.equipment} onChange={(value) => updateEvent('equipment', value)} textarea /></FormSection><div className="form-actions"><button type="button" className="button secondary">Save draft</button><button type="submit" className="button primary">Submit request</button></div></form></div>
}
