import type { EventData } from '../types'
import { Field, Impact, StatusBadge } from '../components/FormControls'

export function ManagePage({ event, updateEvent, onSave }: { event: EventData; updateEvent: (field: keyof EventData, value: string) => void; onSave: () => void }) {
  return <div className="page-stack"><div className="two-column"><section className="panel"><div className="section-heading"><div><p className="eyebrow">EDIT EVENT</p><h2>Event information</h2></div><StatusBadge status="Pending" /></div><Field label="Event title" value={event.eventName} onChange={(value) => updateEvent('eventName', value)} /><Field label="Description" value={event.description} onChange={(value) => updateEvent('description', value)} textarea /><Field label="Purpose" value={event.purpose} onChange={(value) => updateEvent('purpose', value)} textarea /><Field label="Date and time" value={event.preferredStartDate} onChange={(value) => updateEvent('preferredStartDate', value)} /><Field label="Venue request" value={event.venueRequirements} onChange={(value) => updateEvent('venueRequirements', value)} textarea /><button className="button primary" onClick={onSave}>Save changes</button></section><section className="panel"><div className="section-heading"><div><p className="eyebrow">IMPACT CHECK</p><h2>Planning indicators</h2><p className="muted">Placeholder signals for later validation.</p></div></div><Impact label="Venue suitability" value="Review" tone="warning" /><Impact label="Equipment availability" value="Ready" tone="success" /><Impact label="Schedule conflicts" value="No conflicts" tone="success" /><Impact label="Overall impact" value="Medium" tone="warning" /></section></div></div>
}
